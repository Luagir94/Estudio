# Prettier also honours .gitignore, so this file only lists what is tracked
# but must NOT be reformatted.

# Generated by drizzle-kit. The SQL carries `--> statement-breakpoint`
# markers and the meta snapshots are hashed inputs — both are drizzle's to
# write, not ours to restyle.
drizzle/migrations

# Encrypted Pencil design files (see .gitattributes: never normalise these).
design
*.pen

# Local agent tooling state, including git worktrees checked out under
# .claude/worktrees. Nothing here is tracked, and a sibling checkout's files
# are not ours to format (same reason eslint already ignores .atl/).
.claude/
