# Revisions to skip when running `git blame`: bulk, mechanical reformats
# that would otherwise claim authorship of every line they touched.
#
# Enable it once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: format the codebase with prettier
944dc3aebb92d1ceb4347efe5faefe5dd62f3751
